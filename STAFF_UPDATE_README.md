# 🔧 Обновление системы персонала

## Что изменено

### ❌ Старая структура (все видели одинаковые данные):
```json
{
  "employee1": {"name": "Иван", ...},
  "employee2": {"name": "Петр", ...}
}
```

### ✅ Новая структура (у каждого пользователя свои данные):
```json
{
  "Kvv": {
    "employee1": {"name": "Иван", ...}
  },
  "user2": {
    "employee2": {"name": "Петр", ...}
  }
}
```

---

## 📋 Инструкция по обновлению

### 1. Бэкенд (на Render)

#### Вариант A: Автоматическая миграция (рекомендуется)
1. Загрузите обновленный файл `back/app.py` на Render
2. Загрузите скрипт миграции `back/migrate_staff.py`
3. В консоли Render выполните:
   ```bash
   python migrate_staff.py
   ```
4. Перезапустите сервер

#### Вариант B: Ручная миграция
1. Скачайте текущий `data/staff.json` с сервера
2. Измените структуру вручную (см. выше)
3. Загрузите обратно
4. Загрузите обновленный `back/app.py`

### 2. Фронтенд (на Netlify)

Замените только эти файлы:
- ✅ `front/index.html`
- ✅ `front/api-config.js` (если еще не заменили)
- ✅ `front/api-integration.js` (если еще не заменили)
- ✅ `front/token-verification.js` (должен быть подключен)

---

## 🎯 Что это дает

1. **✅ Каждый пользователь видит только своих сотрудников**
2. **✅ Изменения одного пользователя не влияют на других**
3. **✅ Данные сохраняются отдельно для каждого аккаунта**
4. **✅ При выходе и входе данные загружаются корректно**

---

## ⚠️ Важно

- После миграции все существующие сотрудники будут привязаны к администратору `Kvv`
- Другие пользователи начнут с пустого списка персонала
- Бэкап создается автоматически в `data/staff_backup.json`

---

## 🐛 Устранение проблем

### Проблема: Данные не сохраняются
**Решение**: Проверьте консоль браузера (F12), убедитесь что токен сохранен

### Проблема: Вижу чужих сотрудников
**Решение**: Выйдите из аккаунта и войдите снова (Ctrl+F5 для очистки кэша)

### Проблема: Ошибка при загрузке
**Решение**: Проверьте что бэкенд обновлен и миграция выполнена

---

## 📞 Поддержка

Если возникли проблемы - проверьте:
1. Обновлен ли бэкенд (`app.py`)
2. Выполнена ли миграция данных
3. Обновлен ли фронтенд (все 3-4 файла)
4. Очищен ли кэш браузера
