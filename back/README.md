# Hostel Manager - Backend API

Python Flask бэкенд для системы управления хостелом.

## 🚀 Установка и запуск

### Шаг 1: Установка Python
Убедитесь, что у вас установлен Python 3.8 или выше:
```bash
python --version
```

### Шаг 2: Установка зависимостей
```bash
cd back
pip install -r requirements.txt
```

### Шаг 3: Запуск сервера
```bash
python app.py
```

Сервер запустится и покажет адреса для доступа:
- **Локально**: http://127.0.0.1:5000
- **В локальной сети**: http://ваш-IP:5000

## 🌐 Настройка для внешнего доступа

### Доступ из локальной сети
Сервер уже настроен для работы на `0.0.0.0`, что означает доступ из всех сетевых интерфейсов.

1. Узнайте свой локальный IP адрес:
   ```bash
   ipconfig
   ```
   Найдите IPv4 адрес (например: 192.168.1.100)

2. Другие устройства в той же WiFi сети могут подключаться по адресу:
   ```
   http://192.168.1.100:5000
   ```

3. **Важно**: Убедитесь что брандмауэр Windows разрешает входящие подключения на порт 5000

### Настройка брандмауэра Windows
```powershell
# Откройте PowerShell от имени администратора и выполните:
New-NetFirewallRule -DisplayName "Hostel Manager API" -Direction Inbound -Protocol TCP -LocalPort 5000 -Action Allow
```

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/users` - Список пользователей (admin)
- `PUT /api/auth/users/<username>` - Обновить пользователя (admin)
- `DELETE /api/auth/users/<username>` - Удалить пользователя (admin)

### Комнаты
- `GET /api/rooms` - Получить все комнаты
- `POST /api/rooms` - Создать комнату
- `PUT /api/rooms/<id>` - Обновить комнату
- `DELETE /api/rooms/<id>` - Удалить комнату
- `DELETE /api/rooms/all` - Удалить все комнаты (admin)

### Персонал
- `GET /api/staff` - Получить весь персонал
- `POST /api/staff` - Добавить сотрудника
- `PUT /api/staff/<id>` - Обновить сотрудника
- `DELETE /api/staff/<id>` - Удалить сотрудника

### Прочее
- `GET /api/health` - Проверка работоспособности

## 🔐 Авторизация

Для защищенных endpoints добавьте заголовок:
```
Authorization: Bearer <username>
```

## 📁 Структура данных

Все данные хранятся в папке `data/`:
- `users.json` - Пользователи
- `rooms.json` - Комнаты
- `staff.json` - Персонал

## 👤 Администратор по умолчанию

```
Логин: Kvv
Пароль: Kvv08072001
```

## 🔧 Изменение порта

Если порт 5000 занят, измените в `app.py`:
```python
app.run(host='0.0.0.0', port=8080, debug=True)
```

## 🛠 Обновление фронтенда

Чтобы фронтенд работал с бэкендом, добавьте в начало JavaScript кода:
```javascript
const API_URL = 'http://192.168.1.100:5000/api'; // Замените на ваш IP
```

## ⚠️ Важные замечания

1. **Безопасность**: Текущая реализация использует простую аутентификацию. Для продакшена рекомендуется использовать JWT токены.

2. **База данных**: Сейчас используются JSON файлы. Для продакшена рекомендуется PostgreSQL или MySQL.

3. **HTTPS**: Для работы через интернет настройте SSL сертификат.

4. **Порт**: Убедитесь что порт 5000 не занят другим приложением.

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте что Python установлен корректно
2. Убедитесь что все зависимости установлены
3. Проверьте настройки брандмауэра
4. Убедитесь что порт не занят
